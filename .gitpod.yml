tasks:
  - init: >
      sudo cp ./compile /usr/bin
      sudo cp /usr/lib/gcc/x86_64-linux-gnu/9/include/omp.h /usr/include
    command: >
      sudo cp ./compile /usr/bin
      sudo cp /usr/lib/gcc/x86_64-linux-gnu/9/include/omp.h /usr/include